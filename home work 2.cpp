#include <iostream>
#include <cmath>
using namespace std;
    int step (int a, int b int result)
        {
        result=pow(a,b);
        return step;
        }
    int main()
    {
        int a,b,result;
        cin>>a>>b;
        result = step(a,b);
        cout<<result<<endl;
        return 0;
    }
